not sure if calculation is correct input.txt against data.csv

stringstream ss /*
    strValue = "3";
    std::stringstream ss(strValue);
    float value;
    ss >> value;

    Reads characters from "3"
    Converts them into a float
    Stores the result in value
*/